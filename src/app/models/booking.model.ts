export interface Booking {
  bookingId: number;
  movieTitle: string;
  theatreName: string;
  showTime: string;
  seats: string[];
  totalAmount: number;
}
//interface and data types to define the structure of a Booking object